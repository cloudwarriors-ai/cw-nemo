# QA Continuity App Dependencies
#
# Version constraints use compatible release (~=) or upper bounds (<) to prevent
# breaking changes while allowing security patches.
#
# To regenerate with exact versions:
#   pip install pip-tools
#   pip-compile requirements.in --output-file=requirements.txt
#
# Last updated: 2026-01-14

# =============================================================================
# Core dependencies
# =============================================================================
PyGithub>=2.1.0,<3.0  # GitHub API client
requests>=2.31.0,<3.0  # HTTP client
python-dotenv>=1.0.0,<2.0  # Environment variable loading
pydantic>=2.5.0,<3.0  # Input validation for API routes

# =============================================================================
# Bot dependencies (Flask application)
# =============================================================================
flask>=3.0.0,<4.0  # Web framework
gunicorn>=21.0.0,<23.0  # WSGI HTTP server
flask-sock>=0.7.0,<1.0  # WebSocket support for Output Media
openpyxl>=3.1.0,<4.0  # Excel report generation

# =============================================================================
# Testing
# =============================================================================
pytest>=8.0.0,<9.0  # Test framework
pytest-cov>=4.0.0,<6.0  # Coverage reporting
pytest-asyncio>=1.0.0,<2.0  # Async test support

# =============================================================================
# LiveKit + Avatar (Phase 5) - Voice and avatar streaming
# =============================================================================
livekit-agents[simli]>=1.3.0,<2.0  # LiveKit agent framework with Simli
livekit-plugins-deepgram>=1.0.0,<2.0  # Deepgram ASR
livekit-plugins-cartesia>=1.0.0,<2.0  # Cartesia TTS
livekit-plugins-openai>=1.0.0,<2.0  # OpenAI integration

# =============================================================================
# Optional: Zoom Meeting SDK
# =============================================================================
zoom-meeting-sdk>=0.0.25,<1.0  # Zoom meeting integration

# =============================================================================
# Optional: LLM integration (uncomment if needed)
# =============================================================================
# anthropic>=0.18.0,<1.0  # Anthropic Claude API
