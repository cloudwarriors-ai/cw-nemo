{
  "acceptance_checks": [
    "CLI invite creation succeeds.",
    "HTTP invite claim succeeds.",
    "HTTP team summary and brief reflect the same task truth as the CLI coordination loop.",
    "CLI adopt-existing succeeds against seeded legacy data."
  ],
  "allowed_scope": [
    "artifact-loop/scripts/scenarios/org_surface_smoke_v1.sh",
    "artifact-loop/scripts/scenarios/lib/shared-coordination-common.sh",
    "artifact-loop/package.json"
  ],
  "automatable_acceptance": true,
  "cancelled_by": "",
  "classification": "ready",
  "constraints": [
    "Do not edit shared_coordination_v1.sh beyond invoking existing helpers if needed.",
    "Keep adoption validation CLI-only.",
    "Emit a manifest and smoke report."
  ],
  "definition_of_done": {
    "acceptance_checks": [
      "CLI invite creation succeeds.",
      "HTTP invite claim succeeds.",
      "HTTP team summary and brief reflect the same task truth as the CLI coordination loop.",
      "CLI adopt-existing succeeds against seeded legacy data."
    ],
    "allowed_scope": [
      "artifact-loop/scripts/scenarios/org_surface_smoke_v1.sh",
      "artifact-loop/scripts/scenarios/lib/shared-coordination-common.sh",
      "artifact-loop/package.json"
    ],
    "behavior_outcome": "Operators can run one scenario to validate the new organization surfaces end to end before rollout.",
    "evidence_requirements": [
      "manifest",
      "smoke summary",
      "scenario capture"
    ],
    "objective_linkage": "REQ-001",
    "rollback_or_fallback": "Revert the new smoke and helper additions if the scenario cannot remain bounded.",
    "verifier_acceptance_condition": "Reviewer confirms the smoke exercises the intended contracts and keeps shared_coordination_v1 canonical."
  },
  "dependencies": [],
  "dependency_mode": "accepted_upstream",
  "evidence_destination": "planning_artifacts/artifact-loop-stabilization-20260320/packet-build-org-surface-smoke.json",
  "execution_mode": "sequence_required",
  "execution_strategy": "command_capture",
  "failure_signals": [
    "Scenario reimplements too much of the canonical coordination flow.",
    "HTTP route checks are absent or do not validate payload semantics.",
    "Adoption proof depends on service endpoints instead of CLI-only commands."
  ],
  "fallback_or_rollback": "Remove the new smoke script and helper additions if it cannot stay bounded to operator validation.",
  "last_cycle_id": "objective-artifact-loop-stabilization-20260320-cycle-002",
  "last_verifier_output": "accepted",
  "maintainable_completion_path": true,
  "packet_class": "implementation",
  "packet_id": "packet-build-org-surface-smoke",
  "packet_lane": "worker",
  "parallelism_class": "serial",
  "primary_behavior": "Implement the new org stabilization smoke script and minimal shared scenario helpers needed to exercise mixed CLI and HTTP surfaces plus CLI-only adoption flow.",
  "product_meaning_resolved": true,
  "prohibited_action_required": false,
  "runtime_state": "accepted",
  "shared_surface_categories": [
    "scenario",
    "http-service",
    "cli"
  ],
  "strategy_inputs": {
    "command": "cd /Users/chadsimon/chad_bot_attempt/nemoclaw/artifact-loop && npm run scenario:org-surface-smoke"
  },
  "swarm_eligible": false,
  "verifier_mapping": "Review the new smoke script, helper changes, and a passing smoke capture."
}
